import { EmptyDashboardState } from "@/components/dashboard-ui";

export function NonAuthoritativeNotificationNote() { return <aside className="rounded-lg border border-violet-200 bg-violet-50 p-3 text-sm text-slate-700"><strong>Updates indicator:</strong> This page shows current items you can access. It does not track delivery or unread messages, so it is not an authoritative notification count.</aside>; }
export function AnnouncementFeed({ items }: { items: { id: string; title: string; body: string; published_at: string | null }[] }) { return items.length ? <section className="space-y-4" aria-label="Announcements">{items.map((item) => <article key={item.id} className="rounded-xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><h2 className="text-lg font-bold">{item.title}</h2>{item.published_at && <p className="mt-1 text-sm text-slate-600">Published {new Date(item.published_at).toLocaleDateString()}</p>}<p className="mt-3 whitespace-pre-wrap text-slate-800">{item.body}</p></article>)}</section> : <EmptyDashboardState title="No announcements right now">There are no published announcements for your current school relationships and role.</EmptyDashboardState>; }
export function CalendarFeed({ items }: { items: { id: string; title: string; description: string; starts_at: string; ends_at: string }[] }) { return items.length ? <section className="space-y-4" aria-label="Private calendar">{items.map((item) => <article key={item.id} className="rounded-xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><h2 className="text-lg font-bold">{item.title}</h2><p className="mt-1 text-sm font-semibold text-slate-700">{new Date(item.starts_at).toLocaleString()} – {new Date(item.ends_at).toLocaleString()}</p>{item.description && <p className="mt-3 whitespace-pre-wrap text-slate-800">{item.description}</p>}</article>)}</section> : <EmptyDashboardState title="No private events right now">There are no published private events for your current school relationships and role.</EmptyDashboardState>; }
