import { redirect } from "next/navigation";
import { CalendarFeed, NonAuthoritativeNotificationNote } from "@/components/internal-feed";
import { EmptyDashboardState, PortalHeader, UnavailableDashboardState } from "@/components/dashboard-ui";
import { requireRole } from "@/lib/auth/require-role";
import { createClient } from "@/lib/supabase/server";
export const dynamic = "force-dynamic";
export default async function StudentCalendarPage() { if (await requireRole() !== "student") redirect("/dashboard"); const supabase = await createClient(); const { data: claims } = await supabase.auth.getClaims(); if (!claims?.claims.sub) redirect("/login"); const { data: student, error: studentError } = await supabase.from("students").select("id").eq("profile_id", claims.claims.sub).eq("status", "active").maybeSingle(); if (studentError) return <main className="mx-auto max-w-5xl space-y-6"><PortalHeader role="student" eyebrow="Calendar" title="Private school calendar"><p>Events could not be loaded.</p></PortalHeader><UnavailableDashboardState /></main>; if (!student) return <main className="mx-auto max-w-5xl space-y-6"><PortalHeader role="student" eyebrow="Calendar" title="Private school calendar"><p>Your active student record is not available.</p></PortalHeader><EmptyDashboardState title="No active student record">Ask an administrator to link your student record.</EmptyDashboardState></main>; const { data, error } = await supabase.from("events").select("id,title,description,starts_at,ends_at").order("starts_at"); return <main className="mx-auto max-w-5xl space-y-6"><PortalHeader role="student" eyebrow="Calendar" title="Private school calendar"><p>Read-only events available to your active role and class relationships.</p></PortalHeader>{error ? <UnavailableDashboardState /> : <><NonAuthoritativeNotificationNote /><CalendarFeed items={data ?? []} /></>}</main>; }
