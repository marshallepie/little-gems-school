import { AuthForm } from "@/components/auth-form";
export default function LoginPage() { return <AuthForm title="School login" description="Sign in with your approved Little Gems account." buttonLabel="Sign in" mode="login" />; }
