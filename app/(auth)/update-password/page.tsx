import { AuthForm } from "@/components/auth-form";
export default function UpdatePasswordPage() { return <AuthForm title="Update password" description="Set a new password after opening an approved reset link." buttonLabel="Update password" password />; }
